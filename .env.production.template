# Production Environment Configuration
# Copy this to .env.production and fill in your actual values

# Database - Use your production PostgreSQL URL
DATABASE_URL="postgresql://username:password@host:port/database"

# NextAuth.js - CRITICAL for authentication
NEXTAUTH_URL="https://tjoeftjaf.xyz"
NEXTAUTH_SECRET="your-very-secure-secret-here-minimum-32-characters"

# Email Configuration (required for verification and notifications)
EMAIL_USER="your-production-email@gmail.com"
EMAIL_PASS="your-gmail-app-password"

# Cron Secret for automated tasks
CRON_SECRET="your-cron-secret-key"

# Optional: iCloud Calendar integration
# ICAL_URL="your-ical-url"
# ICAL_USERNAME="your-username"
# ICAL_PASSWORD="your-password"

# Node Environment
NODE_ENV="production"

# IMPORTANT NOTES:
# 1. NEXTAUTH_SECRET must be at least 32 characters long
# 2. NEXTAUTH_URL must match your exact domain (https://tjoeftjaf.xyz)
# 3. EMAIL_PASS should be a Gmail App Password, not your regular password
# 4. Keep this file secure and never commit it to version control